//faça um programa que calcule e mostre a área de um triangulo area = (base* altura) /2

const triangulo = (base, altura) => {
    const area = (base * altura) / 2
    return area
}
console.log(triangulo(100, 10));

